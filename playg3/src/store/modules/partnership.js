const state = {
    data: {}
}

const getters = {

}

const mutations = {
    handleUpdatePartnership(state, payload) {
        state.data = payload
    }
}

export default {
    state,
    getters,
    mutations,
}